// 俺の勉強記録 -- TOEIC900点なのに喋れない男の毎日の英語勉強ログ
// 自分の撮影動画だけ載せる。キュレーション側 (grind-log.ts) とは別物。
// Latest entry goes at the TOP of the array.

import type { GrindEntry } from './grind-log';

export type MyStudyEntry = GrindEntry;

export const myStudyEntries: MyStudyEntry[] = [];
